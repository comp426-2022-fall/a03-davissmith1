function roll(sides, dice, timesRolled){
    const results = [];
    for(let i = 0; i < timesRolled; i++){
        rolls[i]= dice*Math.floor(Math.random() * this.sides) + 1 
    }
    return("sides: " + sides +",");
    return("dice: " + dice + ",");
    return("rolls:" + timesRolled + ","):
    return("resulst: " + results);

}